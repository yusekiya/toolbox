#! /usr/bin/env python3
'''
Docstring
'''

import os
import subprocess
import argparse

HOME_DIR = os.path.expanduser('~')
file_path = os.path.join(HOME_DIR, 'scratch', 'test.md')

out = subprocess.run(["vim", file_path])
print(out)
